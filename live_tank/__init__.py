"""Live Tank: camera capture, fish detection, tracking and identities.

`service.service` is the running tracker the web app talks to; `dashboard`
maps its state into the payload the site renders.
"""
